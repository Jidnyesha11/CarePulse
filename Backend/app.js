require("dotenv").config();const express=require("express"),cors=require("cors"),helmet=require("helmet"),morgan=require("morgan"),routes=require("./routes"),{errors}=require("./middleware");const app=express();app.use(helmet());app.use(cors({origin:[process.env.CLIENT_URL,"http://localhost:5173"].filter(Boolean)}));app.use(express.json({limit:"1mb"}));app.use(morgan("dev"));app.get("/api/health",(q,s)=>s.json({ok:true}));app.use("/api",routes);app.use((q,s)=>s.status(404).json({message:"Not found"}));app.use(errors);module.exports=app;