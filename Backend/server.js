require("dotenv").config();const http=require("http"),{Server}=require("socket.io"),app=require("./app"),db=require("./config/db");(async()=>{await db();let server=http.createServer(app),io=new Server(server,{cors:{origin:[process.env.CLIENT_URL,"http://localhost:5173"].filter(Boolean)}});app.set("io",io);io.on("connection",s=>s.emit("connected",{id:s.id}));server.listen(process.env.PORT||5000,()=>console.log("HMS API running"))})().catch(e=>{console.error(e);process.exit(1)});