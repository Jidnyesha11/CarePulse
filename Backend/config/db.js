const mongoose=require("mongoose");
module.exports=async()=>{if(!process.env.MONGODB_URI)throw Error("MONGODB_URI is required");await mongoose.connect(process.env.MONGODB_URI);console.log("MongoDB connected");};