# Validation
Backend Node syntax checks: PASS.
The archive includes dependency manifests but not node_modules. Run `npm install`, `npm test`, and `npm run build` in both apps in your environment before deployment. External MongoDB/SMTP/OpenAI services must also be configured and reachable.
