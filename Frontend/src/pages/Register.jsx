import {useState}from"react";import{Link,useNavigate}from"react-router-dom";import{useAuth}from"../auth";
export default function Register(){const{register}=useAuth(),nav=useNavigate(),[f,setF]=useState({name:"",email:"",password:"",phone:""}),[err,setErr]=useState("");async function submit(e){e.preventDefault();try{await register(f);nav("/dashboard")}catch(x){setErr(x.response?.data?.error?.message||"Registration failed")}}return <div className="auth"><div className="auth-card"><small>PATIENT REGISTRATION</small><h2>Create your account</h2><p className="muted">Appointments, records, prescriptions and bills in one place.</p><form onSubmit={submit}>{["name","email","phone","password"].map(k=><label key={k}>{k==="name"?"Full name":k[0].toUpperCase()+k.slice(1)}<input required={k!=="phone"} type={k==="email"?"email":k==="password"?"password":"text"} minLength={k==="password"?8:undefined} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></label>)}{err&&<div className="error">{err}</div>}<button className="primary">Create account</button></form><p className="muted center-text">Already registered? <Link to="/login">Sign in</Link></p></div></div>}
