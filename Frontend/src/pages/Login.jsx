import {useState}from"react";import{Link,useNavigate}from"react-router-dom";import{HeartPulse}from"lucide-react";import{useAuth}from"../auth";
export default function Login(){const{login}=useAuth(),nav=useNavigate(),[f,setF]=useState({email:"",password:""}),[err,setErr]=useState("");async function submit(e){e.preventDefault();try{await login(f.email,f.password);nav("/dashboard")}catch(x){setErr(x.response?.data?.error?.message||"Sign in failed")}}return <div className="auth"><div className="auth-card"><div className="brand auth-brand"><span className="mark"><HeartPulse size={20}/></span>CarePulse</div><small>SECURE HOSPITAL ACCESS</small><h2>Welcome back</h2><p className="muted">Sign in to your care workspace.</p><form onSubmit={submit}><label>Email<input type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Password<input type="password" required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label>{err&&<div className="error">{err}</div>}<button className="primary">Sign in</button></form><p className="muted center-text">New patient? <Link to="/register">Create an account</Link></p></div></div>}
