import {useEffect,useState}from"react";import{api}from"../api";import{useAuth}from"../auth";import{CalendarDays,CircleDollarSign,Users,BedDouble}from"lucide-react";
const Card=({title,value,icon})=><div className="card"><div className="card-head"><span>{title}</span>{icon}</div><strong className="metric">{value}</strong></div>;
export default function Dashboard(){const{user}=useAuth(),[d,setD]=useState(null);useEffect(()=>{if(user.role==="admin")api.get("/admin/analytics").then(r=>setD(r.data.data));else Promise.all([api.get("/appointments?limit=5"),api.get(user.role==="patient"?"/bills":`/records/${user.id}`)]).then(([a,b])=>setD({appointments:a.data.data.items,secondary:b.data.data}))},[user]);if(!d)return <div className="center">Loading dashboard…</div>;if(user.role==="admin")return <><div className="title"><div><small>OPERATIONS</small><h2>Hospital overview</h2></div></div><div className="metrics"><Card title="Patients" value={d.patients} icon={<Users/>}/><Card title="Doctors" value={d.doctors} icon={<Users/>}/><Card title="Appointments" value={d.appointments} icon={<CalendarDays/>}/><Card title="Revenue" value={`₹${d.revenue}`} icon={<CircleDollarSign/>}/></div><div className="grid"><div className="card"><h3>Bed occupancy</h3><strong className="big">{d.beds.occupied}/{d.beds.total}</strong></div><div className="card"><h3>Recent appointments</h3>{d.recent.map(a=><div className="row" key={a._id}><span>{a.patient?.name}</span><small>{a.date} · {a.timeSlot}</small></div>)}</div></div></>;return <><div className="title"><div><small>{user.role.toUpperCase()} PORTAL</small><h2>Your care workspace</h2></div></div><div className="metrics"><Card title="Appointments" value={d.appointments.length} icon={<CalendarDays/>}/><Card title={user.role==="patient"?"Bills":"Records"} value={d.secondary.length} icon={<FileIcon/>}/><Card title="Security" value="RBAC" icon={<BedDouble/>}/></div><div className="card"><h3>Upcoming care</h3>{d.appointments.map(a=><div className="row" key={a._id}><span>{a.doctor?.name||a.department}</span><small>{a.date} · {a.timeSlot} · {a.status}</small></div>)}</div></>}
function FileIcon(){return <CalendarDays/>}
